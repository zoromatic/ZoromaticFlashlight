apply plugin: 'com.android.application'

android {
    signingConfigs {
        zoromaticflashlight {
            keyAlias 'zoromaticflashlight'
            keyPassword 'zoka2906'
            storeFile file('D:/Work/Software/Android/ZoromaticFlashlight/key')
            storePassword 'zoka2906'
        }
    }
    compileSdkVersion 26
    buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId "com.zoromatic.flashlight"
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 7
        versionName "1.2"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
            signingConfig signingConfigs.zoromaticflashlight
        }
    }
}

dependencies {
    compile 'com.android.support:support-v4:26.1.0'
    compile 'com.android.support:appcompat-v7:26.1.0'
}
